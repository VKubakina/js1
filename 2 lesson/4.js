'use strict'

        /** Функция складывает параметры
        * @param {number} num1
        * @param {number} num2
        * @returns {namber} */
        function addNamber (num1, num2) {
            return num1 + num2;
        }
        /** Функция делит параметры
        * @param {number} num1
        * @param {number} num2
        * @returns {namber} */
        function quotientNanber (num1, num2) {
            return num1 / num2;
        }
        /** Функция отнимает параметры
        * @param {number} num1
        * @param {number} num2
        * @returns {namber} */
        function differenceNumber (num1, num2) {
            return num1 - num2;
        }
        /** Функция перемножает параметры
        * @param {number} num1
        * @param {number} num2
        * @returns {namber} */
        function multiplicationNumber (num1, num2) {
            return num1 * num2;
        }
        alert (addNamber (3, 4));
        alert (quotientNanber (10, 2));
        alert (differenceNumber (8, 5));
        alert (multiplicationNumber (2, 2));
        